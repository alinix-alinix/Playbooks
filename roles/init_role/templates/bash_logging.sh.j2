{{ ansible_managed | comment }}
export HISTTIMEFORMAT='%F %T ' # We cut date duplicate in the $PROMPT_COMMAND to avoid timestamp duplication
export PROMPT_COMMAND='RETRN_VAL=$?;logger -p local6.debug "$(whoami) [$$]: $(history 1 | tr -s " " | cut -d " " -f 5- | sed "s/^[ ]*[0-9]\+[ ]*//" ) [$RETRN_VAL]"'
